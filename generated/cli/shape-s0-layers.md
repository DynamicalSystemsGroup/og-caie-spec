$ uv run -q ogc shape S0-Layers
# ogc shape S0-Layers @ <sha>
## S0-Layers  (shapes/epo.shapes.ttl)
target: epo:ServiceAgreement

property constraints (0):
(none)

sparql constraints (1):
  - S0 two layers (R-32): every item pinned at the contract is generated no later than the
    requirement set, and every item pinned within the evaluation no earlier than the agreement.
(exit 0)
